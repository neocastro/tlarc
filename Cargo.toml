[package]
name = "tlarc"
version = "0.0.0"
edition = "2021"
rust-version = "1.75"
description = "A Rust reimplementation of the TLC model checker for TLA+"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
